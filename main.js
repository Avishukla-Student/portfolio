var options = {
    Strings:['frontend developer','web developer',"Collage Student"],
    typeSpeed:100,
    backSpeed:100,
    backDelay:1000,
    loop:true
};

var typed= new typed(".text", options);
